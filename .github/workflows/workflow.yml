name: PipelineWorkflow

on:
  push:
    branches:
      - main
  pull_request:
    branches: 
      - main

jobs:
    build:
        name: Azure Pipeline
        runs-on: ubuntu-latest
        steps:
        - name: Azure Pipelines Action
          uses: Azure/pipelines@v1
          with:
            azure-devops-project-url: https://dev.azure.com/kashyaproopesh222/AgileProject
            azure-pipeline-name: 'webapp_test'
            azure-devops-token: ${{ secrets.AZURE_DEVOPS_TOKEN }}